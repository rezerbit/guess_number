<%= form_for(@secretnumber) do |f| %>
  <% if @secretnumber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@secretnumber.errors.count, "error") %> prohibited this secretnumber from being saved:</h2>

      <ul>
      <% @secretnumber.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br />
    <%= f.number_field :value %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
